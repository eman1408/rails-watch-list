<h1>New Movie</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit class: 'btn btn-flat'%>
  <%= link_to "Cancel", list_path(@list), class: "btn btn-flat" %>

<% end %>
